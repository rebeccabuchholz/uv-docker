services:

  uv-api:
    container_name: uv-api
    build:
      context: .
    volumes:
      - .:/home/uv-user
    ports:
      - 8010:8000
    healthcheck:
      test: ["CMD", "curl", "-f", "localhost:8000/glow-check"]
      interval: 30s
      timeout: 5s
      retries: 5